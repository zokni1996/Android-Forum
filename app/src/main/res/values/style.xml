<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Progressbar theme-->
    <style name="ProgressBarTheme" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progress_horizontal_holo_light</item>
        <item name="android:minHeight">16dip</item>
        <item name="android:maxHeight">16dip</item>
    </style>

    <!-- Splash theme -->
    <style name="ThemeSplash" parent="android:Theme.Holo.Light.NoActionBar" />

    <!-- Settings theme -->
    <style name="StyleThemeSettings" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/actionBarStyled</item>
        <item name="android:checkboxStyle">@style/CheckBoxAppTheme</item>
        <item name="android:radioButtonStyle">@style/RadioButtonTheme</item>
    </style>

    <style name="RadioButtonTheme" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:button">@drawable/radio_button_check_holo_light</item>
    </style>

    <style name="actionBarStyled" parent="android:Widget.ActionBar">
        <item name="android:actionBarSize">?attr/actionBarSize</item>
        <item name="android:background">@color/green</item>
    </style>

    <style name="CheckBoxAppTheme" parent="android:Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/button_check_holo_light</item>
    </style>

    <!--Drawer theme -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/green_darker</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="StyleThemeDrawer" parent="AppTheme.Base" />

    <!-- Supported actionbar theme -->
    <style name="ToolbarTheme" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/abc_primary_text_material_light</item>
        <item name="actionMenuTextColor">@color/abc_primary_text_material_light</item>
        <item name="android:textColorSecondary">@color/abc_secondary_text_material_light</item>
    </style>

    <!-- Button rounded theme -->
    <style name="ButtonStyleRounded" parent="android:style/Widget.Button">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/button_rounded_holo_light</item>
        <item name="android:padding">0dp</item>
    </style>
</resources>